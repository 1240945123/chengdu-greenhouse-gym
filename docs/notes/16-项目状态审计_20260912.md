# 16 - 项目状态审计（2026-09-12 22:40）

> 范围：`chengdu-greenhouse-gym` 全仓库。目的：在转入论文写作前，确认实验资产完整、无隐藏错误、遗留项清晰。

---

## 0. 结论速览

| 维度 | 状态 |
|---|---|
| 代码 | ✅ 健康。glassgym 60 文件/7820 行；glass_rl 48 脚本/7734 行；结构清晰 |
| 测试 | ✅ **614 用例全通过，零失败**（`lastfailed = {}`）|
| 实验资产 | ✅ 29 个 RL 产物目录 + 经典控制 + 消融 + 五维评估 + P0 汇总，齐备 |
| 运行中 | ⏳ 2 个训练（CrossQ 300k/600k；B1 的 td3→trpo→recppo），均**确认活跃** |
| 文档 | ⚠️ 18 篇 notes，但**编号重复**（04×2、05×2）|
| 阻塞问题 | ⚠️ **README 主线结论已过时**（引用 09-09 修复前数字）|
| 同步 | ⚠️ 5 个 commit 未推送 |

---

## 1. 代码结构

```
chengdu-greenhouse-gym/
├── glassgym/                      # 模型与环境（60 .py / 7820 行）
│   ├── models/GlassGreenhouse/    # 主模型（28 状态/10 控制/228 参数，玻璃温室+9 离散执行器）
│   ├── models/GreenLight/         # 作物模型（Vanthoor）+ 物理基座
│   ├── models/ChengduPhysicsV1-V5/# 早期成都机理模型（V5 为旧主线）
│   ├── environments/              # env 基类与工具
│   └── configs/                   # 默认参数
├── experiments/controllers/glass_rl/   # 实验层（48 .py / 7734 行）
│   ├── glass_env.py               # ★ 核心环境（reward/obs/标定配置集中于此）
│   ├── env_variants.py            # 连续/单离散/掩码 三种动作空间适配
│   ├── classical_controllers.py   # baseline / rule / PID / MPC(一步前瞻)
│   ├── horizon_search.py          # H 步前瞻搜索（H=1 枚举 / H≥2 CEM）
│   ├── multiagent_env.py / train_multiagent.py
│   ├── residual_env.py            # 残差 RL
│   ├── distill_mpc.py / _v2.py    # DAgger 蒸馏 + 归因诊断
│   ├── train_*.py                 # 各算法训练入口（10 个）
│   ├── eval_*.py / benchmark_*.py # 评估（6 个）
│   └── plot_*.py                  # 绘图（8 个）
├── tests/                         # 82 测试文件 / 614 用例（⚠️ 混入 ~9 个非测试脚本）
├── docs/design(122) / experiments(3) / notes(18)
└── results/chengdu_agri_greenhouse_001/real_greenhouse/   # 全部实验结果
```

**评价**：分层干净（模型 / 环境 / 算法 / 评估 / 绘图），单文件职责明确，`glass_env.py` 作为唯一环境配置入口便于复现。

---

## 2. 运行状态（22:40）

### 活跃训练（已用 tb_log 事件文件确认在写）

| 任务 | 进度 | 最后写入 | 备注 |
|---|---|---|---|
| CrossQ | 300k / 600k | 22:40:54 | 每千步约 116s（TQC 为 35s）|
| B1 td3 | 150k / 600k | 22:41:20 | 之后还有 trpo、recurrent_ppo |

### 已完成（本轮 P0 + B 部分）

| 编号 | 内容 | 结果 |
|---|---|---|
| P0-1 | 动作掩码 1536→384 | 舒适率 44.9%（−7.6）、果实 **8.37（最高）**、最高温 31.7（最低）、无效动作 200%→0% |
| P0-2 | TQC | 舒适率 38.9%、**最高温 41.9→33.1、无过热日 90→100%、reward −79→+80** |
| P0-2 | CrossQ | ⏳ 进行中 |
| P0-3 | DAgger 蒸馏 | v1 44.6% / 一致性 42.5%（**负向**）；v2 归因 → **+16.5pp**，证明信息赤字是主因 |
| P0-3推论 | PPO + 未来4h天气（obs16）| **54.8% / 7.71 / 末端 reward 45.8（+16.4）→ RL 历史最佳** |
| P0-4 | H 步前瞻前沿 | H=1 枚举 67.9%；**等候选数下 H=2 仅 +0.2pp 却贵 7.3 倍 → 一步前瞻已近最优** |
| B1 | a2c / dqn / sac / ddpg | ✅ 完成（新协议）|
| B2 | 多智能体 IPPO | 47.0% / 6.51（**低于单智能体 PPO，甚至略低于规则**）|

### 同协议下全策略总表（102 天）

| 策略 | 舒适率 % | 果实 kg/m² | 决策 ms* |
|---|---:|---:|---:|
| 一步前瞻搜索 H=1（同目标）| **67.9** | 7.15 | 88.9 |
| MPC（原实现）| 67.4 | 7.12 | 94.9 |
| DAgger 蒸馏 v2 | 61.1 | 6.37 | — |
| **PPO v9（obs16）** | **54.8** | 7.71 | 1.5 |
| PPO v6（obs8）| 52.5 | 7.28 | 1.1 |
| 规则控制 | 48.8 | 6.94 | ~0.004 |
| 多智能体 IPPO | 47.0 | 6.51 | 1.5 |
| MaskablePPO | 44.9 | **8.37** | 2.3 |
| DAgger 蒸馏 v1 | 44.6 | 7.73 | 1.8 |
| TQC | 38.9 | 6.93 | 0.63 |
| SAC v3 | 35.9 | 6.58 | 0.65 |

\* **延迟均在多任务并发下测得，绝对值为上界，需空闲重测**（见 §4 P0-2）。

---

## 3. 待办清单

| 优先级 | 事项 | 依赖 |
|---|---|---|
| **P0** | 更新 README 主线结论（当前引用的是修复前数字）| 无 |
| **P0** | 空闲重测所有策略决策延迟 | 等训练全部结束 |
| **P0** | 推送 5 个未推送 commit | 无 |
| P1 | 等 CrossQ（600k）+ B1 剩余 3 算法（td3/trpo/recppo）| 训练 |
| P1 | 全量汇总评估：把 B1 矩阵 + CrossQ 并入五维表，重出图 | 上一条 |
| P1 | 写 P0 结论文档（四条 P0 的完整结论）| 上一条 |
| P1 | 论文第 3/5 章成文（按 `14-论文写作蓝图.md`）| 素材已齐 |
| P2 | 文档编号去重、logs/ 清理、孤立产物归档 | 无 |

---

## 4. 问题与风险

### P0-1 ⚠️ README 主线结论已过时

`README.md` 目前写：

> 结论：RL 温湿度控制显著优于人工（舒适率 **48% vs 17%**，最高温 34 vs 47°C，reward 改善 **6.5 倍**）

该数字来自 `results/.../rl/clean_benchmark_result.md`（**2026-09-09**），口径是「CO₂ 修复后 / 坐果初起点 / 5-1 起 72 天」——**早于**后续三项关键修复（冠层过热、外遮阳透光、`solar_gain_scale` 0.91→0.42）与郫都作物标定。

当前口径（102 天 / seedling / 修复后）下的数字是：人工 **33.7%**、PPO v9 **54.8%**、MPC **67.4%**。README 若不改，会与论文正文冲突。

### P0-2 ⚠️ 延迟测量受 CPU 竞争污染

并发 4–7 个任务时测得：PPO v6 **2.22 ms**（空闲 0.70 ms）、SAC **5.78 ms**（空闲 0.43 ms）、MPC **94.9 ms**（空闲 78.2 ms）。比值可用，**绝对值不可用**。「RL 推理成本约为 MPC 的 1/80」这类核心论点必须在空闲状态重测后才可定稿。

### P0-3 ⚠️ 5 个 commit 未推送

`git log origin/main..HEAD` = 5：包含 obs 预报扩展、掩码评估修复、P0 汇总与图。

### P1-1 训练并发过度，off-policy 被拖慢 2.5–4 倍

TQC 600k 耗时 **5h51m**（SAC 单跑同量 139 min）；CrossQ 每千步约 116s（TQC 35s）。经验：off-policy 长训练应独占 CPU。

### P1-2 docs/notes 编号重复

`04-完整生长季_冠层过热bug修复.md` 与 `04-实验计划PLAN.md`；`05-作物参数标定.md` 与 `05-计划评审日志.md`。引用时易混淆。

### P1-3 残留与孤立产物

- `logs/probe.out`（我此前做存活探针留下的空文件），且 `logs/` 未进 `.gitignore`；
- `results/.../rl/sac_final`、`sac_final_v2`：仅 1 个 checkpoint、无 `model.zip`（训练被主动中止）；
- `algorithms_fullseason_legacy`、`glass_ppo*`、`ppo_final`：早期世代，保留作溯源但应标注。

### P2 tests/ 混入非测试脚本

`chengdu_physics_calibration.py`、`data_pipeline.py`、`env_test.py`、`mpc_controller.py`、`pid_controller.py`、`pinn_surrogate.py`、`rule_based_baseline.py`、`transformer_surrogate.py`、`weather_sampling.py` 等位于 `tests/` 但并非 pytest 用例，建议移入 `scripts/` 或 `experiments/`。

### 方法论提醒（本轮踩到的一个坑）

**不能用 `training_curve.csv` 的 mtime 判断训练是否活跃**——该 CSV 只在每个 checkpoint 段（150k）**结束时**写盘。正确信号是 `tb_log/*/*.tfevents.*` 的 mtime。本轮据此先误判 CrossQ "停滞 2h18m"，复查后确认为活跃。

---

## 5. 下一步建议

**主线不变**（依 `13-P0决策_下一步主线.md`：实验收口 + 论文写作，性能优化封板）。建议顺序：

1. **立即（今日）**：修 README 结论 → 清理 `logs/` → 推送 5 个 commit。三项都是低风险高收益。
2. **等训练结束**：CrossQ + B1 剩余 3 算法 → 全量汇总（把新算法并入五维表）→ **空闲重测延迟**（这是唯一影响核心论点的测量问题）。
3. **随即**：写 P0 结论文档，把四条 P0 的统一结论固化——「**瓶颈在信息与目标设定，而非算法复杂度**」（P0-1 动作精简不划算、P0-3 观测比奖励更关键、P0-4 一步前瞻已足够，三者互证）。
4. **然后转论文**：按 `14-论文写作蓝图.md` 从第 3 章（物理数字孪生）或第 5 章（实验）开始成文。
5. **不要做**：继续刷算法性能（`comfort_bonus` 三轮已证伪）、为堆数量扩展算法、真实温室部署（本轮时间不够）。

---

## 附：审计命令留档（可复现）

```bash
export PATH="/usr/bin:/bin:$PATH"        # 本机 Bash 需显式补 coreutils
cd "E:/school/final paper/chengdu-greenhouse-gym"

.venv/Scripts/python.exe -m pytest tests/ -q          # 614 用例
cat .pytest_cache/v/cache/lastfailed                  # {} = 无失败
stat -c '%y' results/.../tb_log/*/*.tfevents.*        # 训练活跃信号
.venv/Scripts/python.exe -m experiments.controllers.glass_rl.eval_p0_bundle
```
